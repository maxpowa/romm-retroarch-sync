# Build-time dependencies for Windows .exe creation

# PyInstaller - creates standalone executables
pyinstaller>=6.0.0

# Runtime dependencies (same as requirements.txt)
requests>=2.28.0
watchdog>=3.0.0
cryptography>=3.4.8
Pillow>=10.0.0
psutil>=5.9.0
